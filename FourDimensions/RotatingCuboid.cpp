#include "Physics4D.h"

RotatingCuboid(Vector4 position, Rotation initialRotation, Vector4 sizes, const sf::Clock& clock, Colorization colorization, Vector4 vectorRotation1, Vector4 vectorRotation2, FPN rotationSpeedAround, FPN rotationSpeedInside) :
	RotatingMesh(position, )
{ }
